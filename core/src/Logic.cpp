#include "Logic.h"
namespace Core
{

    bool Logic::check(SignalVariant signal)
    {

        switch (msignalType)
        {
        case SignalType::key:

            break;

        default:
            break;
        }
    }
}